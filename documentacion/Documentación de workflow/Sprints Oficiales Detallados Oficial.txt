================================================================================
WORKSÍ — SPRINTS OFICIALES DETALLADOS (DOCUMENTO OFICIAL)
================================================================================
Propósito de este archivo:
Guiar a todo el equipo, incluso con poca experiencia, sprint por sprint, sin
dejar tareas “a criterio” sin definir. Cada bloque dice QUÉ hacer, POR QUÉ,
en QUÉ orden aproximado, QUÉ se considera “terminado” y QUÉ está prohibido
según la documentación oficial del proyecto (Sprint oficiales, Contrato
Unificado API, Reglas del Proyecto, HU sin épicas, Stack Oficial, Documento
técnico, Flujo Pantallas App Web y Móvil).

Fuente de verdad del calendario y de las horas por sprint: Sprint oficiales.txt del mismo folder
Cuando algo no esté aquí explícito, se aplica documento Reglas del Proyecto

Convenciones en este documento:
- “Equipo” = 3 integrantes. “Horas del sprint” = esfuerzo TOTAL del equipo en
  esa semana (no horas por persona).
- “MVP obligatorio” = debe quedar listo para la entrega salvo indicación
  explícita de backlog opcional.
- “Solo si sobra tiempo” = NO consume el compromiso del MVP; no bloquea otros
  entregables.

Regla global de matching (válida en todo el proyecto; no negociar en código):
- El candidato declara y persiste skills en perfil (HU-04, HU-05).
- El porcentaje de compatibilidad en producto considera: CV limpio (texto extraído
  y fairness cuando corresponda) frente texto semántico oferta donde job_text se arma
  en backend sólo con title + description + modality + workload según Contrato; más política combinada en producto cuando preferencias modality/workload del candidato aplican contra la oferta (Sprint oficiales + Contrato).
- El contrato mínimo hacia IA es POST /match con cv_text y job_text. En este
  documento, la palabra “skills” en reglas de porcentaje designa las listas
  estructuradas guardadas en BD: skills de perfil del postulante y skills
  requeridas declaradas en la ficha de la oferta. Esas skills de perfil y de
  oferta NO se concatenan a cv_text ni a job_text para influir en el score, NO
  se envían como campos adicionales en el request de POST /match y NO modifican el porcentaje de matching.
- Lo anterior no confunde con el texto libre del CV: si dentro del PDF el
  postulante describe habilidades, herramientas, tecnologías o competencias en
  prosa (por ejemplo una sección de competencias o el detalle de experiencias),
  ese contenido forma parte del texto del CV tras Tika y fairness y SÍ cuenta
  para la similitud semántica frente a job_text. No se trata de “skills de
  perfil”; es contenido narrativo del propio CV.
- Tampoco influyen en el porcentaje: presentación personal del perfil, rango de
  sueldo esperado, sueldo ofrecido en la oferta, años de experiencia requeridos
  por la oferta, años de experiencia declarados dentro del CV, nacionalidad.

Regla global de testing (válida en todo el proyecto):
- No implementar testing en frontends: no carpetas de testing en el proyecto web (React/Vite) ni en el móvil (Android); quienes desarrollan esas capas comprueban que todo funcione visualmente y por uso manual.
- La fase de pruebas con alcance definido no se reparte como bloque continuo en cada sprint: al final del MVP, con el sistema completo integrado, se ejecuta solo un conjunto acotado de pruebas de las más críticas para verificar que las partes esenciales encajan (integración entre servicios, flujo core, E2E, y comprobación puntual con Postman sobre endpoints críticos si el equipo lo usa en esa pasada). Durante los sprints, los apartados de este documento que hablen de “pruebas” o “Postman” en tareas B4 o similares se entienden como comprobaciones mínimas puntuales si hacen falta para desbloquear backend o integración, no como montaje de suites en frontend ni como sustituto de esa verificación final conjunta.
- Evitar testing opcional o de baja prioridad.





================================================================================
ANTES DEL SPRINT 1 — ACUERDOS QUE TODO EL EQUIPO DEBE ENTENDER
================================================================================

1) Entornos:
- Docker Compose con MySQL + Spring Boot + FastAPI es obligatorio en el plan;
  Android corre fuera de Docker.
- MySQL 8 + Flyway Community para migraciones SQL versionadas. Java 21. Spring Boot 3.2.x. Python 3.11 para FastAPI.

2) No crear tabla matches: el matching se calcula dinámicamente

3) Autenticación oficial
- JWT simple: solo access token, sin refresh, sin OAuth, un filtro JWT en
  Spring Security.

4) CV oficial
- Archivo en filesystem del servidor; en MySQL solo referencia o ruta o URL;
  no guardar el binario del PDF en MySQL.
- MVP: solo PDF con texto seleccionable; sin OCR; mensaje claro si el PDF no
  entrega texto útil.
- En app móvil de postulantes: tamaño máximo 1 MB.

5) Fairness (cuando toque en Sprint 6)
- spaCy NER básico: PERSON, ORG, GPE; regex para patrones estructurados.
- Orden operativo: regex inicial → spaCy sobre texto libre → regex final de
  cierre.
- No eliminar ORG indiscriminadamente; priorizar institución de procedencia
  sin borrar contexto profesional útil.

6) Estados de postulación
- APPLIED, VIEWED, CANCELLED existen como dominio; en MVP móvil lo visible es
  APPLIED y VIEWED. CANCELLED solo si sobra tiempo (HU-14).
- VIEWED solo por acción manual del reclutador en sesión RECRUITER (HU-23).
  Prohibido marcado automático de VIEWED.

7) Lista oficial “solo si sobra tiempo” (no planificar como bloqueante)
- HU-24 Guardar oferta
- HU-16 Editar oferta
- HU-17 Eliminar oferta (descartada del diseño actual por estado único ACTIVE)
- HU-14 Cancelar postulación
- HU-18 Activar/desactivar oferta (descartada del diseño actual por estado único ACTIVE)
- HU-22 Eliminación de cuenta





================================================================================
SPRINT 1 — SEMANA 20 AL 26 DE ABRIL DE 2026 (SPRINT PESADO)
================================================================================
Esfuerzo total estimado del equipo: 10 horas (suma de las 3 personas en la
semana).

Objetivo del sprint en una frase:
Dejar el proyecto arrancable con Docker Compose, MySQL y cadena Flyway ejecutable
hasta los scripts vigentes del repositorio (dominio oficial: users CANDIDATE/ADMIN/RECRUITER,
companies sin credencial, recruiter_profiles.company_id, jobs según Contrato); Spring Boot
operativo con GET /health (Spring donde exista en el sprint) y catálogos públicos GET /api/v1/catalogs/* donde apliquen; esqueleto FastAPI cuando exista en compose.
HU-01 (registro multipart candidato cerrado) es Sprint 4; en Sprint 1 solo stubs opcionales
tras BD estable si alcanza tiempo. Política de contraseña conocida equipo (12 caracteres,
símbolo, número, mayúsculas y minúsculas) para implementaciones posteriores.

------------------------------------------------------------
A) DEFINICIÓN DE “TERMINADO” DEL SPRINT 1 (Definition of Done)
------------------------------------------------------------
Al cerrar el sprint, debe cumplirse TODO lo siguiente:

1) Infraestructura base
- Existe docker-compose funcional para al menos: MySQL, contenedor/servicio
  backend Spring Boot (aunque sea salud básica), y la estructura preparada
  para FastAPI aunque FastAPI aún no haga matching (puede exponer health del servicio, testeable mediante comandos curl).
- MySQL levanta con volumen persistente o estrategia acordada para no perder
  datos en cada reinicio de desarrollo.
- Documentación mínima en README del repo: cómo levantar compose, puertos,
  variables de entorno mínimas (usuario/clave DB, URL JDBC, etc.).

2) Esqueleto backend
- Proyecto Spring Boot 3.2.x con paquetes claros (por ejemplo controller,
  service, repository, entity, dto, config, security si aplica ya o
  parcialmente).
- Conexión JPA a MySQL verificada (entidad de prueba o entidad User mínima).
- Manejo básico de errores: respuestas HTTP coherentes cuando falla validación
  o reglas de negocio (no silenciar errores).

3) Migraciones Flyway y esquema (prioridad Sprint 1)
- Cadena ejecutable limpia: migraciones hasta el nivel definido en el repositorio
  (referencia Sprint oficiales: V1 dominio principal, V2 catálogo, rol ADMIN en users,
  recruiter_profiles, jobs con company_id, ajustes comerciales y salary/experience donde
  apliquen).
- Flyway ejecuta al levantar Spring; JPA ddl-auto coherent con validate donde esté configurado.

4) HU-01 y validaciones multipart (solo si sobra después de infra y BD)
Opcional tras DoD infra: stubs alineados a Contrato Unificado API (auth/candidate) o endpoint
ligero de validación de campos multipart según equipo; alta oficial multipart con consentimiento
queda Sprint 4. Flujo cliente sin persistencia candidato antes de HU-21: según Flujo Pantallas App Móvil.
Si existe servicio que valide formato email, política contraseña, RUT antes del alta oficial,
mantener resultado controlado ante error (mensaje estable, no stacktrace crudo).

Criterio de aceptación transversal:
- Criterio de equipo: ejecutar migrate en base limpia reproducible desde compose según Sprint oficiales.



------------------------------------------------------------
B) DESGLOSE ULTRA DETALLADO (orden sugerido para minimizar bloqueos)
------------------------------------------------------------

B1) Tarea 1 — Flyway y dominio físico oficial (prioridad; 3 a 4 horas equipo)
- Escribir o verificar carpeta migraciones hasta scripts vigentes repo: users con
  ENUM CANDIDATE/ADMIN/RECRUITER, password_hash solo login, companies sin credencial,
  recruiter_profiles.company_id, jobs alineadas Contrato (sin campo requirements separado),
  aplicaciones swipe/guardados según modelo, seed ADMIN con hash, catálogo V2, etc.
- DoD: Subir aplicación contra MySQL vacío y confirmar historia Flyway hasta última versión.

B2) Tarea 2 — Docker Compose “feliz path” (2 a 3 horas equipo)
- Levantar MySQL con usuario, password, base de datos y puerto fijos acordados.
- Definir volumen para datos de MySQL.
- Definir volumen o carpeta persistente futura para CV (aunque CV se implemente
  previa a persistir CV en Sprint 4, puede crearse carpeta montada ya para no rearmar compose).
- Spring Boot contenedor o local: si es local, igual debe existir compose de
  MySQL estable; documentar ambos modos si se usa mixto.

DoD compose:
- Un compañero nuevo puede clonar y levantar con los pasos documentados sin
  llamar a otro integrante.


B3) Tarea 3 — Entidades JPA alineadas a migraciones (2 a 3 horas)
- Reconciliar entidades y repositorios con esquema Flyway aplicado (User, CandidateProfile,
  Company, Job, recruiter_profiles, etc.; sin tablas onboarding).
- Restricción única email en users donde exista en BD.

B4) Tarea 4 — Health y catálogos mínimos (1 a 2 horas)
- GET /health o equivalente del backend (operación salud sin prefijo /api/v1 en implementación actual); GET /api/v1/catalogs/regions y GET /api/v1/catalogs/regions/{region_id}/communes (y otros catálogos públicos mínimos del Contrato)
  para desbloquear Sprint 3 movil cuando consuma servidor.
- No implementar alta multipart candidato aquí si el equipo prioriza esquema; documentar POST
  multipart Sprint 4 en Contrato.
- Sin persistencia de fila candidato hasta consentimiento y multipart oficial (HU-21 / HU-01 cierre Sprint 4).

B5) Tarea 5 — Verificación cruzada (1 a 2 horas)
- docker compose up: MySQL healthy, backend arranca Flyway OK, opcional ai /health curl.
- Postman opcional: catálogos, health y un caso feliz donde exista validación provisional.


------------------------------------------------------------
C) QUÉ NO HACER EN EL SPRINT 1 (para no romper el plan)
------------------------------------------------------------
- No implementar matching ni FastAPI completo aún si “sobra tiempo”: el orden
  oficial exige CV y oferta antes (Sprint oficiales).
- No agregar chat, notificaciones, dashboards, Kafka, colas, microservicios
  extra.
- No guardar PDF en MySQL.
- No implementar HU opcionales.


------------------------------------------------------------
D) Reparto sugerido de roles (flexible; ajustar a habilidades reales)
------------------------------------------------------------
- Integrante A: Compose + MySQL + variables + documentación de arranque.
- Integrante B: migraciones Flyway + entidades alineadas + repositorios.
- Integrante C: controllers GET /health + catálogos GET /api/v1/catalogs/* donde existan en el sprint + verificación compose + Postman mínimo.





================================================================================
SPRINT 2 — SEMANA 27 DE ABRIL AL 3 DE MAYO DE 2026
================================================================================
Esfuerzo total estimado del equipo: 10 horas.
Contenido oficial: HU-03 Login unificado + HU-02 Alta ficha empresa y reclutadores
por ADMIN según modelo oficial (Contrato Unificado API, HU sin épicas, Flujo Pantallas App Web).

Objetivo del sprint en una frase:
JWT operativo para CANDIDATE, ADMIN y RECRUITER; ADMIN puede dar de alta companies
solo datos negocio (sin usuario empresa ni credencial empresa) y usuarios RECRUITER
con company_id; bloqueo por intentos fallidos y recuperación mock alineados web/móvil.

------------------------------------------------------------
A) DEFINICIÓN DE “TERMINADO” DEL SPRINT 2
------------------------------------------------------------
1) HU-03 Login
- POST /api/v1/auth/login único para web (ADMIN y RECRUITER) y candidato donde aplique Contrato.
- Respuesta JWT solo access token con claims rol CANDIDATE | ADMIN | RECRUITER.
- Tras 4 intentos fallidos bloqueo y derivación a flujo recuperación mock (alineación Flujo web/móvil).
- Credenciales incorrectas 401 mensaje seguro sin filtrar existencia correo más allá práctica común Contrato.
- Logout lado cliente donde exista cliente (elimina token, estado no autenticado).
- Persistencia token en cliente: documentar opción equipo (web/mobile mínimo).

2) HU-02 Alta empresa y reclutadores (no registro público empresa)
- No existe POST público empresa en MVP web obligatorio ni rol COMPANY para login empresa.
- Con ADMIN Bearer: POST /api/v1/admin/companies multipart (parte datos negocio según HU; sin correo/clave empresa).
- GET /api/v1/admin/companies para listado/desplegable al registrar reclutadores.
- POST /api/v1/admin/recruiters con company_id existente, email reclutador, password bajo política (12 caracteres,
  símbolo, número, mayúsculas y minúsculas), unicidad email y campos HU.
- Persistencia unicamente password_hash para usuarios con login.

3) Seguridad Spring
- Rutas públicas POST /api/v1/auth/login, registros públicos pactados en Contrato (p. ej. POST /api/v1/auth/register/candidate cuando aplique); GET /health sin prefijo /api/v1 donde exista en Spring; catálogos públicos GET /api/v1/catalogs/*.
- Rutas /api/v1/admin/* protegidas rol ADMIN.
- Opcional mismo sprint: skeleton POST verify/reset recuperación si sobra tiempo (Sprint oficiales).

------------------------------------------------------------
B) DESGLOSE ULTRA DETALLADO
------------------------------------------------------------
B1) Servicio seguridad JWT + filtros Spring (3 a 4 horas)
- PasswordEncoder bcrypt; Jwt generation con sub, role, email, exp.
- Carga usuario por email; roles desde users.role.

B2) Admin companies + recruiters multipart (3 a 4 horas)
- Validaciones HU-02; persistencia row companies sin credencial usuario empresa.
- recruiter_profiles enlazadas a users RECRUITER y company_id elegida.

B3) Lockout recuperación stub (2 horas si prioridad)
- Incremento failed_login_attempts, lock_until; mock verify/reset mínimo alineado flujos.

B4) Logout cliente (1 hora)
- Web cuando exista; móvil si ya hay pantalla login; si no existe UI web aún checklist Sprint 7.

B5) Postman feliz path (1 hora)
- login ADMIN seed + crear company + recruiter + login RECRUITER + 401 recurso ADMIN sin rol.

------------------------------------------------------------
C) QUÉ NO HACER EN EL SPRINT 2
------------------------------------------------------------
- No construir modelo “usuario empresa” con mismo login que reclutador: companies es fila negocio.
- No usar vacantes anuales ni workflow antiguo register/company público salvo backlog explícito fuera Contrato MVP.






================================================================================
SPRINT 3 — SEMANA 4 AL 10 DE MAYO DE 2026
================================================================================
Esfuerzo total estimado del equipo: 9 horas (puede rozar un poco más).
Contenido oficial: HU-06 (CV en flujo móvil antes de "Uso de datos") + HU-04
(captura de skills, rubro, presentación y preferencias en flujo) + catálogos
necesarios; sin persistir candidato en backend hasta el alta oficial multipart
(según Contrato Unificado API y Flujo Pantallas App Móvil).

Objetivo del sprint en una frase:
Alinear app móvil (y apoyo de API) al flujo: datos y CV en cliente,
skills/rubro/preferencias en cliente, catálogos en servidor; preparar criterios
y límites (PDF 1 MB) documentados; persistencia de CV y perfil en BD se realiza
en Sprint 4 con un solo POST /api/v1/auth/register/candidate (multipart).

------------------------------------------------------------
A) DEFINICIÓN DE “TERMINADO” DEL SPRINT 3
------------------------------------------------------------
1) Flujo móvil (prioridad)
- Pantallas Subir CV / Mostrar CV: archivo PDF solo en dispositivo hasta
  "Uso de datos"; validación de tamaño y tipo en cliente (Contrato/Flujo).
- Pantallas Skills y Últimas preferencias: sector_id, skills_ids (3..12),
  sueldos y preferencias en estado local, coherentes con el JSON "data" del
  registro.
- No existe POST /onboarding/* ni filas de borrador en MySQL.

2) HU-06 (producto móvil)
- El almacenamiento en filesystem y referencia en BD se implementa al crear la
  cuenta (Sprint 4), en la misma transacción del multipart de alta.

3) HU-04
- Misma lógica: lo capturado se envía al backend en el "data" del multipart
  al aceptar consentimiento; skills de perfil no entran al score de matching.

4) API de apoyo
- GET /api/v1/catalogs/sectors y GET /api/v1/catalogs/sectors/{sector_id}/skills
  (si aún faltan), públicos, alineados al contrato.

Reglas: sin cuenta CANDIDATE en BD hasta POST /api/v1/auth/register/candidate; sin
onboarding_id ni fusión: un solo request de alta.

------------------------------------------------------------
B) DESGLOSE ULTRA DETALLADO
------------------------------------------------------------
B1) App móvil: estado unificado (ViewModel/estado) para datos de registro, CV
  y skills (2 a 3 horas).

B2) Validaciones cliente: PDF, 1 MB, 3..12 skills, rango de sueldo (2 a 3 h).

B3) Catálogos en backend: sectores y skills por sector si aplica (1 a 2 h).

B4) Documentación: ejemplo de request multipart (part "data" + "file")
  hacia POST /api/v1/auth/register/candidate para Sprint 4.

B5) Verificación: recorrido Crear cuenta -> ... -> "Uso de datos" sin llamadas
  que creen fila de candidato; solo catálogos.

------------------------------------------------------------
C) QUÉ NO HACER EN EL SPRINT 3
------------------------------------------------------------
- No calcular matching aún.
- No crear ofertas aún (Sprint 5).
- No implementar tablas o endpoints de borrador servidor (contrato oficial: alta
  candidato en un paso multipart con consentimiento en Sprint 4).






================================================================================
SPRINT 4 — SEMANA 11 AL 17 DE MAYO DE 2026
================================================================================
Esfuerzo total estimado del equipo: 10 horas.
Contenido oficial: HU-21 Consentimiento de datos + alta oficial del candidato +
perfil autenticado (un solo POST multipart que persiste todo lo recopilado en
cliente).

Objetivo del sprint en una frase:
Al aceptar en "Uso de datos", crear en un solo request la cuenta CANDIDATE,
persistir CV y perfil (POST /api/v1/auth/register/candidate multipart) y dejar
operativo GET/PATCH /api/v1/candidate/profile.

------------------------------------------------------------
A) DEFINICIÓN DE “TERMINADO” DEL SPRINT 4
------------------------------------------------------------
1) HU-21 Consentimiento de uso de datos
- Checkbox obligatorio en pantalla "Uso de datos" como última pantalla antes
  del alta oficial.
- Si no acepta, no se crea cuenta.
- Si acepta, se crea cuenta CANDIDATE y se emite access_token.
- Persistencia en BD de consentimiento: flag + timestamp.

2) Alta oficial en un paso (Contrato Unificado API vigente)
- POST /api/v1/auth/register/candidate con Content-Type multipart/form-data:
  parte "data" (JSON con consent_given=true, identidad, sector_id, skills_ids,
  sueldos, preferred_modalities, preferred_workloads, etc.) y parte "file"
  (PDF del CV, max 1 MB).
- Transacción: usuario, perfil, skills, preferencias, CV en filesystem +
  metadatos en BD; sin onboarding_id ni fusión de borrador.

3) Perfil autenticado posterior al alta
- GET/PATCH /api/v1/candidate/profile operativo y coherente con lo persistido
  en el alta.
- Rutas /api/v1/candidate/* con Bearer access_token rol CANDIDATE.

------------------------------------------------------------
B) DESGLOSE ULTRA DETALLADO
------------------------------------------------------------
B1) Endpoint POST /api/v1/auth/register/candidate (3 a 4 horas)
- Parseo multipart, validaciones, persistencia atómica, emisión JWT CANDIDATE.

B2) Modelo de consentimiento en cuenta final (1 a 2 horas)
- consent_given + consent_given_at consistentes.

B3) Perfil autenticado candidato (2 a 3 horas)
- GET/PATCH /api/v1/candidate/profile sobre cuenta creada.

B4) Pruebas críticas (1 a 2 horas)
- Caso sin consentimiento: no se llama al endpoint o rechazo controlado.
- Caso con consentimiento + multipart válido: alta exitosa y acceso a Perfil.
- Verificar ausencia de rutas /onboarding/* en el flujo final.

------------------------------------------------------------
C) QUÉ NO HACER EN EL SPRINT 4
------------------------------------------------------------
- No saltarse "Uso de datos" para crear cuentas.
- No implementar fairness todavía.
- No implementar POST /onboarding/candidate/* (obsoleto).







================================================================================
SPRINT 5 — SEMANA 18 AL 24 DE MAYO DE 2026 (SPRINT PESADO)
================================================================================
Esfuerzo total estimado del equipo: 10 horas.
Contenido oficial:
- Extracción de texto con Apache Tika en Spring Boot + post-procesamiento
  mínimo (saltos de línea, ruido básico, consistencia).
- HU-15 Crear oferta laboral.
- HU-10 Ver ofertas.

Objetivo del sprint en una frase:
Extraer texto de CV en backend para el pipeline posterior; tener ofertas ACTIVE
publicadas por RECRUITER según Contrato con job_text bien definido; feed/listado
candidato HU-10 listo antes de fairness y swipe.

------------------------------------------------------------
A) DEFINICIÓN DE “TERMINADO” DEL SPRINT 5
------------------------------------------------------------
1) Extracción Tika
- Tras alta candidato con archivo o bajo demanda acordada, el backend obtiene texto del PDF almacenado.
- Post-procesamiento mínimo obligatorio: normalizar saltos, remover ruido básico, consistencia.
- Si el PDF no entrega texto útil: error controlado por API donde aplique.

2) HU-15 Crear oferta (portal empresa, sesión RECRUITER según Contrato)
- POST /api/v1/company/jobs con Bearer RECRUITER; la oferta se asocia a company_id del reclutador
  y debe persistirse quien es el reclutador publicador para listados/detalle sólo sobre ofertas propias
  según Contrato Oficial del repositorio.
- Campos obligatorios Contrato HU-15 Flujo Pantallas: company_commercial_name; title;
  description único texto largo (no hay tabla requirements persistida aparte ni segundo recuadro);
  city; region_id y commune_id coherentes catálogo; salary_offered y years_experience_required obligatorios;
  modality y workload enums; skills_ids entre 3 y 8 en job_skills.
- Opcional por oferta image_url/imagen archivo según Flujo Contrato si el equipo ya modela esa parte en Sprint 8 UI.
- job_text construido en backend para IA concatena únicamente title + description + modality + workload (no incluye skills,
  salary_offered ni years_experience_required ni nombre comercial en texto semántico).
- PATCH /api/v1/company/profile/image operativo donde corresponda (marca empleador sobre companies, fuera texto job_text).

3) HU-10 Feed / ver ofertas candidato
- GET /api/v1/candidate/jobs/feed (y detalle donde aplique Contrato): solo ACTIVE, sin repetir swipeadas como reglas oficiales.
- Respuesta suficiente para cards Flujo Pantallas incluyendo match score ya integrable si existe servicio llamada interna Sprint 6 o stub controlado hasta integrar.

------------------------------------------------------------
B) DESGLOSE ULTRA DETALLADO
------------------------------------------------------------
B1) Servicio TextExtractionService (3 a 4 horas)
- Integración Tika; persistir texto derivado donde el modelo lo defina fila/metadata cv.
- Salida estable para llamada fairness Sprint 6.

B2) Capa Jobs + vínculos (3 a 4 horas)
- jobs.company_id FK; publicador RECRUITER (user id o recruiter_profile id según modelo tablas Sprint 1); job_skills; validaciones unicidad/coherencia comuna-región.

B3) Endpoints reclutador /company/jobs POST + GET lista filtrando ofertas publicadas por el reclutador autenticado (2 horas).

B4) Endpoints candidato feed + campo match en ítem donde diseño aplique combinación Sprint 6 o placeholder documentado.

B5) Verificación curl/Postman: crear empresa+reclutador precondición Sprint 2, publicar oferta RECRUITER, candidato lista feed.

------------------------------------------------------------
C) QUÉ NO HACER EN EL SPRINT 5
------------------------------------------------------------
- No calcular score aún.
- No hacer swipe aún.







================================================================================
SPRINT 6 — SEMANA 25 AL 31 DE MAYO DE 2026 (SPRINT PESADO)
================================================================================
Esfuerzo total estimado del equipo: 10 horas.
Contenido oficial:
- HU-09 Eliminación de datos sensibles (fairness) en FastAPI con diseño acordado
  (spaCy + regex, orden y solapamiento).
- HU-07 Calcular compatibilidad.
- HU-08 Explicación del match (mínima; plantilla tipo “Coincidencia en: X, Y”).
- Contrato POST /match con cv_text y job_text; sin inyectar listas de skills de
  perfil ni listas de skills de oferta en los textos usados para score.
- En producto, el porcentaje final además considera modalidades y cargas
  horarias preferidas del postulante comparadas con oferta.

Objetivo del sprint en una frase:
Congelar el núcleo IA: texto limpio → fairness → embedding → score base →
ajuste por preferencias → explicación breve y segura, invocable desde Spring.

------------------------------------------------------------
A) DEFINICIÓN DE “TERMINADO” DEL SPRINT 6
------------------------------------------------------------
1) HU-09 Fairness
- Remover/filtrar atributos sensibles objetivo: nombre, edad, género,
  nacionalidad, institución de procedencia, correo, RUT y años de experiencia
  declarados dentro del CV, según reglas.
- Implementación acordada:
  - Regex inicial
  - spaCy NER (PERSON, ORG, GPE) sobre texto libre
  - Regex final de cierre
  - Solapamiento selectivo en nombre, institución, nacionalidad
- No eliminar ORG masivamente: criterio documentado de “procedencia” vs
  “empresa útil”.

2) HU-07 Calcular compatibilidad
- Mostrar porcentaje 0 a 100 en respuesta de producto (puede ser entero).
- Cálculo IA basado en embeddings con modelo all-MiniLM-L6-v2 y cosine
  similarity en scikit-learn.
- Debe respetar exclusiones de matching del perfil y oferta (no sueldos, no
  años exp, etc.).

Definición operativa obligatoria (sin ambigüedad para skills):
- Las skills guardadas en BD en el perfil del postulante y las skills guardadas
  en BD en la oferta (listas o catálogo estructurado) NO forman parte del
  porcentaje de matching: no se añaden a cv_text ni a job_text para el cálculo
  del score y no se envían como campos extra a POST /match.
- job_text que se envía a FastAPI se construye en Spring concatenando únicamente
  título + descripción + modalidad + workload (carga horaria) de la oferta, según Contrato oficial.
  Quedan explícitamente fuera del texto semántico de job_text: listas skills de oferta persistidas en BD,
  sueldo ofrecido, años de experiencia requeridos, company_commercial_name (marca sólo fuera texto IA).
- cv_text que se envía es el texto del CV post-Tika y post-fairness (idealmente
  fairness en Python; si el equipo decide fairness solo en Python, el Java
  envía texto Tika limpio básico y Python aplica fairness antes de embeddings).
  Ese cv_text incluye el contenido narrativo completo del CV, inclusive
  descripciones de habilidades, herramientas o stack escritas por el postulante
  dentro del documento; eso no son las “skills” de perfil en BD y SÍ puede
  influir en la similitud semántica frente a job_text.


3) HU-08 Explicación del match
- Debe basarse en CV vs oferta y preferencias aplicables.
- No usar lista de skills del perfil ni lista de skills de la oferta como fuente
  del score ni de la explicación como si fueran “match por skills de BD”.
- Sí pueden reflejarse temas inferidos del contraste entre el texto del CV
  (incluidas habilidades descritas en prosa en el CV) y el texto permitido de
  la oferta en job_text.
- No exponer datos sensibles en explanation.

4) Contrato HTTP
POST /match
Request:
{
  "cv_text": "...",
  "job_text": "..."
}
Response:
{
  "score": <float 0..1 o int 0..100 acordado>,
  "explanation": "texto breve"
}

Spring Boot:
- Cliente HTTP hacia FastAPI con timeout.
- Manejo de error si IA cae: respuesta controlada al cliente móvil/web.

5) Regla de combinación de score (clarificación obligatoria para el equipo)
- FastAPI retorna score semántico base por textos.
- Spring aplica ajuste por modalidad y carga horaria comparando preferencias
  del candidato vs oferta, produciendo porcentaje final. Documentar fórmula
  mínima (aunque sea lineal con tope) para evitar discusiones en Sprint 11.

------------------------------------------------------------
B) DESGLOSE ULTRA DETALLADO
------------------------------------------------------------
B1) FastAPI proyecto mínimo (2 horas)
- Dockerfile o servicio compose estable.
- GET /health del servicio FastAPI (o Spring según compose) y POST /match del servicio FastAPI (path HTTP /match, sin prefijo /api/v1 del monolito).

B2) Pipeline IA (4 a 5 horas)
- fairness module
- embeddings module
- similarity score
- explanation template generator

B3) Integración Spring (3 a 4 horas)
- MatchClient
- Service que obtiene cv_text almacenado + construye job_text + llama Python
- Endpoint candidato: GET match para ofertaId (o POST) que devuelva score final
  + explanation

B4) Prueba crítica exigida por Sprint 11 (adelantar si se puede)
- Mismo CV y misma oferta debe dar mismo score aunque cambien solo las skills de
  perfil en BD: preparar caso Postman desde ya.
- Mismo CV y misma oferta debe dar mismo score aunque cambien solo las skills
  en job_skills persistidas sin alterar los campos que componen job_text (title, description,
  modality, workload): preparar caso equivalente.

------------------------------------------------------------
C) QUÉ NO HACER EN EL SPRINT 6
------------------------------------------------------------
- No crear tabla matches.
- No usar Kafka/colas.
- No enviar skills de perfil del candidato en el request de POST /match ni anexarlas a cv_text.
- No incluir en job_text el campo o lista de skills requeridas de la oferta.







================================================================================
SPRINT 7 — SEMANA 1 AL 7 DE JUNIO DE 2026 (SPRINT PESADO)
================================================================================
Esfuerzo total estimado del equipo: 10 horas.
Contenido oficial:
- Frontend web MVP Stack Oficial React+Vite: login unificado ADMIN/RECRUTADOR según Contrato Flujo Pantallas Web;
  Portal administrador registrar empresa (sin credencial empresa) y registrar reclutadores; navegación básica;
  sesión reclutador con shell UI (cabeceras, placeholders de listados reproducibles desde API donde falten datos).
- App móvil candidato en paralelo.
- HU-11 Swipe de ofertas.
- HU-12 Confirmar postulación (POST applications tras APPLY).
- Opcional menor completar mocks UI recuperación web/móvil si alcanza.

Objetivo del sprint en una frase:
Swipe y post con score ya integrado; primera web ADMIN/RECRUITER operativa para alta negocio y cuentas reclutadores
según modelo oficial; APIs estables detrás.

------------------------------------------------------------
A) DEFINICIÓN DE “TERMINADO” DEL SPRINT 7
------------------------------------------------------------
1) App móvil (Kotlin) mínimo viable alineado a Flujo Pantallas
- Login con bloqueo tras 4 intentos fallidos y derivación a recuperación en ambos frontends (móvil y web):
  implementación MVP acorde a HU-03 y Flujo Pantallas.
- Recuperación mockup: sin correo real; flujo UI puede completarse; al fijar
  nueva contraseña en ese flujo, misma política de complejidad que en registro
  (mínimo 12 caracteres, al menos un símbolo, al menos un número, mayúsculas y
  minúsculas), alineada a HU sin épicas y Flujos de pantalla.
- Registro de candidato y perfil puede estar parcial si ya existe por API, pero debe
  ser coherente: lo crítico de este sprint es swipe + confirmación + consumo
  de score.

2) HU-11 Swipe
- Swipe derecha → intención de postular.
- Swipe izquierda → descartar y siguiente oferta.
- No repetir ofertas ya vistas/descartadas/postuladas: definir regla simple en
  BD (tabla views/swipes) o filtrar por applications; documentar cuál.

3) HU-12 Confirmar postulación
- Al swipe postular, modal “¿Seguro de postular?” con cancelar/confirmar.
- Solo crea application si confirma.

4) Backend applications
- POST crear postulación idempotente o validada: no duplicar misma oferta para
  mismo usuario; error claro.
- Estados: crear en APPLIED.

5) Web ADMIN + reclutadores (MVP tramo oficial)
- Login único ADMIN/RECRUITER desde misma vista; rutas después según JWT.
- Panel ADMIN funcional para alta multipart empresa y alta reclutador con POST /api/v1/admin/companies, GET /api/v1/admin/companies, POST /api/v1/admin/recruiters según Contrato.
- Shell sesión reclutador: placeholders listados pueden consumir endpoints ya existentes bajo prefijo /api/v1/company/ (p. ej. GET /api/v1/company/jobs) incluso con datos vacíos tratados en UX.

------------------------------------------------------------
B) DESGLOSE ULTRA DETALLADO
------------------------------------------------------------
B1) Endpoint lista ofertas para swipe (1 a 2 horas)
- Incluye campos UI Flujo Pantallas: imagen si existe, nombre, empresa, sueldo,
  comuna, modalidad, años exp (mostrar pero no usar en score), descripción
  truncada, 4 skills, score + barra.

B2) Endpoint detalle oferta (1 a 2 horas)
- Incluye descripción completa, hasta 8 skills, carga horaria, explicación match
  simple.

B3) Android UI (4 a 5 horas)
- Recycler/ViewPager o implementación simple de swipe.
- Observabilidad: loading, error IA, lista vacía.

B4) Confirmación modal (1 hora)

B5) Retrofit + interceptores JWT (1 a 2 horas)

B6) Web React+Vite (4 a 5 horas en paralelo a móvil)
- Proyecto Vite; vistas login ADMIN/RECUITER consumiendo mismo backend.
- Rutas ADMIN companies/recruiters formularios HU-02 multipart.
- Sesión reclutador shell placeholders según Sprint oficiales Flujo Pantallas Web.

------------------------------------------------------------
C) QUÉ NO HACER EN EL SPRINT 7
------------------------------------------------------------
- No marcar VIEWED automáticamente al postular.
- No implementar HU-14 cancelación si roba tiempo.







================================================================================
SPRINT 8 — SEMANA 8 AL 14 DE JUNIO DE 2026
================================================================================
Esfuerzo total estimado del equipo: 9 horas.
Contenido oficial:
- HU-13 Ver postulaciones candidato estados APPLIED / VIEWED mínimos.
- Completar web reclutador: listados GET /api/v1/company/jobs, formulario creación y visualización
  HU-15 alineados Flujo Pantallas App Web (region_id coherence comunas, skills topes Contrato/UI).
- PATCH imagen empresa vía cliente si backlog UI Sprint 7 dejó pendiente marca employer.

Objetivo del sprint en una frase:
Historial candidato coherente; reclutador gestiona ciclo medio ofertas vía pantallas Contrato antes de vistas postulantes finas Sprint 9.

------------------------------------------------------------
A) DEFINICIÓN DE “TERMINADO” DEL SPRINT 8
------------------------------------------------------------
1) HU-13
- Lista de postulaciones del candidato autenticado.
- Muestra nombre oferta, empresa, sueldo ofrecido, estado.
- Estados visibles MVP: APPLIED y VIEWED (VIEWED puede aparecer solo después de
  Sprint 9, pero la UI debe soportarlo sin romper; mientras tanto, VIEWED se
  verá cuando el reclutador publicador marca VIEWED HU-23).

2) Pantalla “Vista Previa Oferta” según Flujo Pantallas
- Desde lista, ver detalle resumido + estado + botón ir a oferta en feed.

3) Web reclutador complemento HU-15
- Listados mismos filtros sólo-ofertas-publicadas-por-el-reclutador autenticado según Contrato.
- Forms publish flow campos opcionales requeridos región/coherencias skills 3–8.

------------------------------------------------------------
B) DESGLOSE ULTRA DETALLADO
------------------------------------------------------------
B1) GET /api/v1/candidate/applications?page=1&size=20&sort=applied_at,desc (1 a 2 horas)
B2) UI móvil lista postulaciones (3 a 4 horas)
B3) Navegación a detalle vista previa oferta (2 a 3 horas)
B4) Web pantallas reclutador listado ofertas + crear oferta HU-15 (3 a 4 horas equipo)
B5) PATCH /api/v1/company/profile/image UI si backlog (1 hora)
B6) Pruebas: postulación creada y listada más roundtrip alta oferta desde web (1 hora)






================================================================================
SPRINT 9 — SEMANA 15 AL 21 DE JUNIO DE 2026
================================================================================
Esfuerzo total estimado del equipo: 9 horas.
Contenido oficial: HU-19 Ver postulantes sesión RECRUITER + HU-23 Marcar VIEWED sólo manual
reclutador + pulimiento navegaciones web segunda mitad panel errores campo “Completa este campo”
según Sprint oficiales Flujo Contrato.

Objetivo del sprint en una frase:
Reclutador ve postulantes por oferta propia administrada sólo sobre jobs que él publicó Contrato,
marca vista manual HU-23; UI mensajes errores formulacion web mejorada Sprint oficiales.

------------------------------------------------------------
A) DEFINICIÓN DE “TERMINADO” DEL SPRINT 9
------------------------------------------------------------
1) HU-19
- Lista candidatos por oferta seleccionada.
- Información básica visible para evaluador humano (panel reclutador; no confundir
  con texto que fairness retira del lado matching automático Contrato IA).
- Sin errores de carga.

2) HU-23
- Acción explícita UI + PATCH /api/v1/company/applications/{application_id}/viewed; sólo el reclutador publicador autorizado Contrato puede ejecutar PATCH.
- Solo cambia la fila seleccionada; servidor sin marcado automático VIEWED idempotencia según Contrato.
- Estado reflejo hu-13 cuando candidato lista postulaciones (refresh navegador o pull).

3) Pulimiento errores navegaciones web segunda mitad HU
- Mensajes “Completa este campo” alineados formularios web portal según HU sin épicas Flujo donde aplique tiempo restante equipo.

------------------------------------------------------------
B) DESGLOSE ULTRA DETALLADO
------------------------------------------------------------
B1) GET /api/v1/company/jobs/{job_id}/applications con auth sólo reclutador publicador + validación company job FK (3 horas).
B2) PATCH VIEWED manual HU-23 (2 horas).
B3) Web UI reclutador listas postulantes + botón marcar visto (3 horas).
B4) Ensayo permisos cruzados: recruiter compañía A vs job empresa B debe 403; segundo reclutador misma company_id no lista job ajeno sólo Contrato reclutador publicador (1 hora).

------------------------------------------------------------
C) QUÉ NO HACER EN EL SPRINT 9
------------------------------------------------------------
- No ordenar aún por compatibilidad (eso es Sprint 10, HU-20).






================================================================================
SPRINT 10 — SEMANA 22 AL 28 DE JUNIO DE 2026
================================================================================
Esfuerzo total estimado del equipo: 7 horas.
Contenido oficial: HU-20 Orden descendente compatibilidad listados API/UI reclutador + HU-05 Editar perfil candidato +
afinar textos seguridad/consentimiento y revisiones menor copy web/móvil según Sprint oficiales.

Objetivo del sprint en una frase:
Reclutador orden postulantes score final producto HU-20 robusto perfil PATCH candidato HU-05 sin romper invariantes matching texto legal copy menor.

------------------------------------------------------------
A) DEFINICIÓN DE “TERMINADO” DEL SPRINT 10
------------------------------------------------------------
1) HU-20
- Lista postulantes ordenada descendente por score.
- Score visible y consistente con IA/regla combinada.
- Definir de dónde sale el score: recalcular on-read llamando IA o guardar
  último score calculado en postulación; cualquiera debe ser consistente y no
  violar reglas. Documentar elección.

2) HU-05 Editar perfil
- El candidato puede modificar campos del perfil incluyendo skills.
- Cambios persistentes.
- No se pierden datos previos al editar parcialmente (definir PATCH robusto).

Regla crítica:
- Editar skills de perfil NO debe cambiar score si CV y oferta no cambian:
  validar con prueba manual tipo Sprint 11.
- Cambiar solo las skills declaradas job_skills tampoco altera porcentaje final si job_text invariante title+description+modality+workload (skills fuera texto semántico Contrato).

3) Pulimiento texto legal menor copy
- Textos seguridad consentimiento HU-21 pantallas correlacionadas revisión menor copy equipo producto stakeholder donde alcance tiempo Sprint oficiales.

------------------------------------------------------------
B) DESGLOSE ULTRA DETALLADO
------------------------------------------------------------
B1) Ordenamiento query o sort service (2 a 3 horas)
B2) UI reclutador sorting visible HU-20 (2 a 3 horas)
B3) Pantalla editar perfil móvil (3 horas)







================================================================================
SPRINT 11 — SEMANA 29 DE JUNIO AL 5 DE JULIO DE 2026 (SPRINT PESADO)
================================================================================
Esfuerzo total estimado del equipo: 8 horas.
Contenido oficial:
- Integración end-to-end candidato-empresa en entorno común.
- Corrección de bugs críticos.
- Checklist de calidad previo al cierre.
- Chequeo explícito de regla de matching Sprint oficiales: mismo CV + misma oferta → mismo
  resultado aunque solo cambien skills de perfil o skills enumeradas job_skills manteniendo
  job_text intocable title+description+modality+workload.
- Pasada integración Spring ↔ FastAPI acotada Postman rutas prioritarias Spring: POST /api/v1/auth/login (multicuenta), POST /api/v1/admin/companies, GET /api/v1/admin/companies, POST /api/v1/admin/recruiters, endpoints /api/v1/company/* del flujo reclutador según Contrato, GET /api/v1/candidate/jobs/feed; servicio IA: POST /match (FastAPI, path /match del contenedor IA).
- Pruebas de integración finales, bugs críticos demo seca, preparación entrega stakeholder.
- HU-22 permanece sólo tiempo extra backlog Sprint oficiales.

Objetivo del sprint en una frase:
Demostrable, estable en flujo core, con evidencia de pruebas críticas y sin
deuda bloqueante.

------------------------------------------------------------
A) CHECKLIST OBLIGATORIA DE CIERRE (sin atajos)
------------------------------------------------------------
1) Flujo E2E mínimo demostrable
- Alta ficha empresa y reclutadores vía ADMIN (POST /api/v1/admin/companies multipart y POST /api/v1/admin/recruiters) o datos semilla coherentes demo.
- Candidato: flujo móvil con CV/datos/skills cliente, HU-21 consentimiento, POST /api/v1/auth/register/candidate multipart; cuenta CANDIDATE operativa perfil Bearer.
- Con CV procesado texto Tika, reclutador publica oferta vía sesión RECRUITER (portal web combinado Contrato demo).
- Candidato consume feed swipe con score HU-11 y confirma postulación HU-12 (POST /api/v1/candidate/applications).
- Mismo reclutador publicador lista applications job marca VIEWED HU-23 candidato ve estado HU-13.
- Lista postulantes orden descendente HU-20 verificación visual.

2) Chequeo explícito regla matching (obligatorio documentado)
- Capturas o logs: dos consultas con misma oferta y mismo CV; en el medio se
  cambian skills de perfil; el porcentaje final no debe cambiar.
- Caso análogo: mismo CV mismos textos IA; sólo mutan filas job_skills sin cambiar campos concatenados job_text Contrato anterior.

3) Seguridad mínima
- Rutas privadas protegidas.
- JWT operativo.

4) Docker Compose
- Levanta en máquina limpia con README.

5) Lista de bugs críticos = vacía o justificada
- Crítico: caídas, 500 en flujo core, datos cruzados entre empresas, login
  roto, postulación duplicada incorrecta, score inconsistente con reglas.

6) Demo alineada a regla global
- Explicar en demo: score explicado como texto del CV (incluidas habilidades
  descritas en prosa en el CV) frente a texto permitido de la oferta en
  job_text, más modalidades preferidas y cargas horarias preferidas; las skills
  de perfil y las skills requeridas en ficha de oferta existen en BD pero no
  gobiernan el porcentaje.

------------------------------------------------------------
B) ENTREGABLES DE DOCUMENTACIÓN PARA DEFENSA (mínimo)
------------------------------------------------------------
- Diagrama simple arquitectura Spring → FastAPI.
- Defensa fairness: regex + spaCy antes de embeddings.
- Defensa almacenamiento CV filesystem + referencia BD + Tika.
- Limitación MVP: sin OCR.






================================================================================
APÉNDICE A — MAPEO RÁPIDO HU → SPRINT (MVP OBLIGATORIO)
================================================================================
Sprint 1 — Infra (Compose, Spring, FastAPI scaffold), cadena Flyway oficial repo; stubs auth/candidate opcional; HU-01 cerrada Sprint 4.
Sprint 2 — HU-03 login multicuenta; HU-02 empresa+reclutadores vía ADMIN; lockout recuperación mock opcional; password_hash sólo usuarios login.
Sprint 3 — Registro cliente móvil sin persistir hasta multipart; HU-06 HU-04 cliente; GET /api/v1/catalogs/* públicos Contrato.
Sprint 4 — HU-21 consentimiento; POST /api/v1/auth/register/candidate multipart; GET /api/v1/candidate/profile y PATCH /api/v1/candidate/profile con Bearer CANDIDATE.
Sprint 5 — Tika; HU-15 crear oferta RECRUITER modelo Contrato actual; PATCH /api/v1/company/profile/image; HU-10 GET /api/v1/candidate/jobs/feed.
Sprint 6 — HU-09 fairness; HU-07 score; HU-08 explicación; contrato técnico POST /match (FastAPI) con cv_text y job_text.
Sprint 7 — Web MVP React+Vite login ADMIN/RECRUITER, panel empresa/reclutadores, shell reclutador; HU-11 HU-12 opcional mocks recuperación.
Sprint 8 — HU-13 postulaciones candidato; completar portal reclutador HU-15 UI; PATCH imagen empresa si backlog.
Sprint 9 — HU-19 HU-23 vistas postulantes solo publicador Contrato + pulimiento mensajes errores web panel segunda mitad.
Sprint 10 — HU-20 orden descendente HU-05 perfil menor copy seguridad/consentimiento.
Sprint 11 — E2E Docker; bugs críticos; invariante matching; Postman rutas Sprint oficiales; demo stakeholder; HU-22 backlog tiempo extra.

================================================================================
APÉNDICE B — BACKLOG “SOLO SI SOBRA TIEMPO” (NO BLOQUEA MVP)
================================================================================
HU-24, HU-16, HU-17 (descartada por estado único ACTIVE), HU-14, HU-18 (descartada por estado único ACTIVE), HU-22

================================================================================
FIN DEL DOCUMENTO
================================================================================
